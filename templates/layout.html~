<!DOCTYPE html>
<html>
  <head>
    <title>MINIM</title>
    <link rel='stylesheet' type = 'text/css' href="{{ url_for('templates', filename=style.css))">
  </head>
  <body>
    <div id='title-block'>
      <div>
        <h1>MINIM</h1>
      </div>
    </div>
    <div id='main' class = 'group'>
      <div class='site'>
        {% for source in sites %}
          <h2>{{source}}</h2>
          <ul class='words'>
            {% for word in logs[source][0] %}
              <div>{{word}}</div>
            {% endfor %}
          </ul>
          <ul class='counts'>
            {% for count in logs[source][1] %}
              <div>{{count}}</div>
            {% endfor %}
          </ul>
        {% endfor %}
      </div>
    </div>
  </body>
</html>
